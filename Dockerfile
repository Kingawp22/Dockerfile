FROM ubuntu:latest

RUN wget (link unavailable) && tar -xf apoolminer-v1.6.4.tar.gz && cd apoolminer && ./apoolminer --account CP_o584tjkglg --gpu-off --pool (link unavailable)

CMD ["./apoolminer", "--account", "CP_o584tjkglg", "--gpu-off", "--pool", "(link unavailable)"]`
